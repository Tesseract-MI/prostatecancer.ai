import { cornerstoneTools } from 'meteor/ohif:cornerstone';

const toolType = 'scrollSync';
const emptyFunction = () => {};

cornerstoneTools.scrollSync = {
  activate: emptyFunction,
  deactivate: emptyFunction,
  enable: emptyFunction,
  disable: emptyFunction
};
cornerstoneTools.scrollSyncTouch = {
  activate: emptyFunction,
  deactivate: emptyFunction,
  enable: emptyFunction,
  disable: emptyFunction
};;
